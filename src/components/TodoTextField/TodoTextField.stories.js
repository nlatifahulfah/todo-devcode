import TodoTextField from ".";

export default {
  title: "TodoTextField",
  component: TodoTextField,
};

const Template = (args) => <TodoTextField {...args} />;

export const Default = Template.bind({});
